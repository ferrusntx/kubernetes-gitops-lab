apiVersion: v1
kind: Service
metadata:
  name: nginx-gitops-service
  namespace: dev-app
spec:
  type: NodePort
  selector:
    app: nginx-gitops
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30107
